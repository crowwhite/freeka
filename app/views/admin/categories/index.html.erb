<%= render 'shared/notice' %>
<%= render 'shared/users/sign_up_in' %>
<h1>Listing categories</h1>
<h3><a id='sub_categories_link'>Show Sub Categories</a></h3>
<%= javascript_include_tag 'sub_categories' %>

<div class='link right'>
  <%= link_to 'New Category', new_admin_category_path %>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <%= render 'show_edit', locals: { category: category } %>
      </tr>
      <% if category.sub_categories.count > 0 %>
        <tr class="sub_category hiddenn">
          <td colspan = 4>
            <table class="table sub_table">
              <thead>                
                <tr>
                  <th>Sub-Category Name</th>
                  <th colspan="3"></th>
                </tr>
              </thead>
              <tbody>
                <!-- TODO Eager load sub-categories -->
                <% category.sub_categories.order(:name).each do |sub_category| %>
                  <tr>
                    <!-- TODO -> Use same partial for both category and subcategory -->
                    <%= render 'show_edit', locals: { category: sub_category } %>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </td>
        </tr>
      <% end %>
    <% end %>
  </tbody>

</table>
