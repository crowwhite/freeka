<p id="notice"><%= notice %></p>

<p>
  <strong>Name:</strong>
  <%= @category.name %>
</p>

<p>
  <strong>Parent Category:</strong>
  <%= @category.parent_category.try(:name) || 'No Parent' %>
</p>

<%= link_to 'Edit', edit_admins_category_path(@category) %> |
<%= link_to 'Back', admins_categories_path %>
