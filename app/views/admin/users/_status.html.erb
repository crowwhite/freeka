<% if locals[:user].enabled %>
  <%= content_tag :td, id: 'status' + locals[:user].id.to_s do %>
    <%= link_to 'disable', admin_user_toggle_status_path(locals[:user], user: { enabled: false }), method: :put, remote: true %></td>
  <% end %>
<% else %>
  <%= content_tag :td, id: 'status' + locals[:user].id.to_s do %>
    <%= link_to 'enable', admin_user_toggle_status_path(locals[:user], user: { enabled: true }), method: :put, remote: true %></td>
  <% end %>
<% end %>