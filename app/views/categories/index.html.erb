<%= render 'shared/notice' %>
<%= render 'shared/users/sign_up_in' %>
<!-- Fixed -> The above thing should be in a different partial. So, can be used in any other location. -->
<!-- Fixed -> Indentation issues -->
<h1>Listing categories</h1>
<h3><%= link_to 'Show Sub Categories', categories_path, remote: true %></h3>

<div class='link right'>
  <%= link_to 'New Category', new_category_path %>
</div>

<table class="table table-striped">
  <thead>
    <tr>
      <th>Name</th>
      <th colspan="3"></th>
    </tr>
  </thead>

  <tbody>
    <% @categories.each do |category| %>
      <tr>
        <td><%= category.name %></td>
        <td><%= link_to 'Show', category %></td>
      </tr>
      <% if category.sub_categories.count > 0 %>
        <tr class="hiddenn">
          <td colspan = 4>
            <table class="table sub_table">
              <thead>                
                <tr>
                  <th>Sub-Category Name</th>
                  <th colspan="3"></th>
                </tr>
              </thead>
              <tbody>
                <% category.sub_categories.order(:name).each do |sub_category| %>
                  <tr>
                    <td><%= sub_category.name %></td>
                    <td><%= link_to 'Show', sub_category %></td>
                  </tr>
                <% end %>
              </tbody>
            </table>
          </td>
        </tr>
      <% end %>
    <% end %>
  </tbody>

</table>
